git status -s  # 查看工作区有哪些文件没被跟踪以及哪些文件的修改没有被记录
git log --oneline # 查看提交记录
1. git add -A # 把所有没被跟踪的文件跟踪了，把所有的修改都记录下来
2. git commit -m "提交注释"   # 提交本次对整个工作空间代码做出的修改（以及添加的新文件）
3. git push  # 把最新的版本节点推送到远程仓库
